# Dependency khusus pengembangan & pengujian.
# Install: uv pip install --python .venv/bin/python -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
httpx==0.27.2          # dibutuhkan fastapi.testclient
debugpy==1.8.5        # debugger Python untuk konfigurasi launch VSCode
